import Foundation

public enum MessageRoutes {
    public static let sendMessage = "messages.send"      // <-- change to match your server
    public static let sendFile   = "messages.sendFile"   // <-- change to match your server
}
